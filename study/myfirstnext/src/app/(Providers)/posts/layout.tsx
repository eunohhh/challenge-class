function PostsLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <div>여기는 레이아웃</div>
            {children}
        </div>
    );
}

export default PostsLayout;
